import { withInstall } from "@nue-ui/utils";
import Icon from "./icon.vue";
import type { IconPropsType, IconNameType } from "./types";

export const NueIcon = withInstall(Icon);
export type { IconPropsType, IconNameType };
