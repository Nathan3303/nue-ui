import { withInstall } from '@nue-ui/utils';
import Overlay from './overlay.vue';
import type { NueOverlayProps } from './types';

export const NueOverlay = withInstall(Overlay);
export type { NueOverlayProps };
