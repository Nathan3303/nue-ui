# Icon Names

All 106 available icon names for `NueIcon` and component `icon` props.

Usage: `<NueIcon name="search" />` or `<NueButton icon="plus">Add</NueButton>` renders `<i class="iconfont icon-search">`.

```
account          arrow-left-more    blank            calendar-wrap     chat
ai-chat-fill     arrow-right        board            check             circle
archive          arrow-right-circle calendar         clear             clear-circle
archive2         arrow-right-more   calendar2        clear-circle-fill coffee
arrow-down       arrow-up           calendar3        completed         connection
arrow-down-circle arrow-up-circle
arrow-left
arrow-left-circle

delete           fail               help             invoices          logo
download         files              history          kanban            mail
drive            filter             inbox            list              menu
edit             focus2             inbox2-fill      loading           menu-close
error-fill       focus3             inbox-empty      location          menu-open
eye                                   inbox-fill       lock              more
eye-close                             in-progress                        more2
                                                                         more-vertical

naotodo-logo     plus-circle-fill   restore-bin      square-check      tag
overdue          priority-1         ring             square-check-fill theme
picture          priority-2         scan             subscribe         time
plus             priority-3         search           success           todo
plus-circle      projects           search2          success-fill      todo-logo-3
                 recycle-bin        select           sync-filled       tomorrow
                 refresh            setting          table             unarchive
                 restore            settings-fill                       update
                                    share                              upload
                                                                       user

warning          week
```

Note: `loading` is the default for loading states (used by Button, SelectOption, Switch). `blank` is the default icon name.
